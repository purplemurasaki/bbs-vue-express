name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy (skeleton)
        run: 'echo "CD skeleton: deploy steps will be implemented in later steps."'

